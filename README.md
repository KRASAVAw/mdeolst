
  # Recreate hero section text

  This is a code bundle for Recreate hero section text. The original project is available at https://www.figma.com/design/bH4HSKJlNUwDhiDRSD6GUc/Recreate-hero-section-text.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  